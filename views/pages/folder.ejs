<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Folder</title>
  </head>
  <body>
    <p><%= folder.name %></p>
    <ul>
      <% folder.children.forEach((child) => { %>
      <li>
        <p><%=child.name%></p>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
